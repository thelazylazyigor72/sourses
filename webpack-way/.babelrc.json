{
    "presets": [
        [
            "@babel/preset-env" 
        ],
        [
            "@babel/preset-react",
            {
                "throwIfNamespace": false, // defaults to true
                "runtime": "automatic" // defaults to classic / auto imports
            }
        ],
        [
            "minify"
        ]
    ]
}